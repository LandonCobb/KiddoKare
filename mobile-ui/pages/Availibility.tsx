import { Text, SafeAreaView } from "react-native";

const Availability = () => {

    return(
        <SafeAreaView>
            <Text>Availability</Text>
        </SafeAreaView>
    );
};

export default Availability;