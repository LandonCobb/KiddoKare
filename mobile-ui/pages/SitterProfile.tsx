import { Text, SafeAreaView } from "react-native";

const SitterProfile = () => {
    return (
        <SafeAreaView>
            <Text>Sitter Profile</Text>
        </SafeAreaView>
    );
};

export default SitterProfile;
