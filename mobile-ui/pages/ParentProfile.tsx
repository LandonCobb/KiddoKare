import { Text, SafeAreaView } from "react-native";

const ParentProfile = () => {
    return (
        <SafeAreaView>
            <Text>Parent Profile</Text>
        </SafeAreaView>
    );
};

export default ParentProfile;
