spring:
  data:
    mongodb:
      uri: ${MONGO_URI}
  application:
    name: parent-auth-service
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL}
  instance:
    prefer-ip-address: true
